package com.company;

public class Main {


    public static void main(String[] args) {

        /** Good job. I'd review for loops to add functionality and UX to your application, as that was the biggest flaw. That and splitting up the
         * logic via multiple methods. Great job, though. */

        MainMenu menu = new MainMenu();


        /** Delete un-used code that has no chance of being used in the future. */
//        Tasks task = new Tasks();

//        menu.taskArrayList();
        menu.selectionMenu();






    }
}
